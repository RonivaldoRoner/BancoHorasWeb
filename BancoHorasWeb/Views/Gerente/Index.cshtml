@model IEnumerable<BancoHorasWeb.Models.Gerente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Funcionario.CPF)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Funcionario.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Funcionario.CPF)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Funcionario.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GerenteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GerenteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GerenteId })
        </td>
    </tr>
}

</table>
